{
  "name": "opencode-docker",
  "version": "0.3.0",
  "description": "Dev container configuration for using Docker and OpenCode",
  "scripts": {
    "projects:generate": "bash scripts/generate-compose-project-mounts.sh",
    "compose:config": "npm run projects:generate && docker compose -f docker-compose.yml -f docker-compose.projects.yml config",
    "up": "npm run projects:generate && docker compose -f docker-compose.yml -f docker-compose.projects.yml up -d",
    "down": "docker compose -f docker-compose.yml -f docker-compose.projects.yml down",
    "down:volumes": "docker compose -f docker-compose.yml -f docker-compose.projects.yml down -v",
    "logs": "docker compose -f docker-compose.yml -f docker-compose.projects.yml logs -f",
    "rebuild": "npm run projects:generate && docker compose -f docker-compose.yml -f docker-compose.projects.yml up -d --build",
    "shell": "docker compose -f docker-compose.yml -f docker-compose.projects.yml exec opencode /usr/local/bin/entrypoint.sh zsh",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "release": "release-it",
    "release:patch": "release-it --release-as patch",
    "release:minor": "release-it --release-as minor",
    "release:major": "release-it --release-as major",
    "release:dry": "release-it --dry-run",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.1",
    "@commitlint/config-conventional": "21.0.1",
    "@release-it/conventional-changelog": "11.0.0",
    "husky": "9.1.7",
    "oxfmt": "0.50.0",
    "release-it": "20.0.1"
  }
}
