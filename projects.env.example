# One project per line: container_folder=host_path
#
# Rules:
# - Left side becomes the folder name under /workspace in the container.
# - Right side must point to an existing host directory.
# - Relative paths are resolved from this file's directory.
#
# Examples:
# app=/Users/you/Developer/app
# api=../api
# docs=../docs
